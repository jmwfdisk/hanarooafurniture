# CLAUDE.md — `hanaro/staff/`

Detail for the 임직원 전용 페이지 (`staff.html`, ~15.5k lines, 9 inline `<script>` blocks) — the single biggest file in the repo and the only one hosting these modules. Loaded **in addition to** the root `CLAUDE.md`; read that first for deployment, cache-bust, Firebase Auth, Firestore collections, and the repo-wide Key Patterns (board writes via `commitBoardChange`, Storage `cacheControl`, XSS escaping, centered dialogs) that this page must follow.

Everything below lives in `staff.html` unless stated otherwise. Historical rationale for these designs is in `docs/개발일지.md` (newest first).

## Permissions (`users.permissions`, `userCan` / `asrPerm`)

Permission helper `userCan(area)` (areas: `all/notice/cert/company/photos/schedule/asResult/asAssignee/asApprStaff/asApprove/asApproveCeo/inventory/materials/leaveSchedule/leaveApprove/leaveApproveFactory/report/hr/pay`) gates UI; admin overrides — the 결재 areas (`asApprStaff`/`asApprove`/`asApproveCeo`/`leaveApprove`/`leaveApproveFactory`) are checked via `asrPerm()`, which passes admins automatically but has **NO `all`-permission override** (the 권한설정 modal can't grant them to admin accounts anyway). Stored in `users.permissions`. Areas are data-driven via `PERM_AREAS` (drives the 권한설정 modal checkboxes `#perm-<area>`); the modal rows show a parenthetical `.perm-desc` describing each area's operations (쓰기/읽기/수정/삭제). The modal groups rows under **category chips** (`#perm-cat-nav`, `setPermCat`): 회사운영/직원게시판/A/S 처리결과/운영관리·사진첩 via `data-cat` on each `.perm-row`, with the '전체' row pinned above the chips — display filter only; hidden rows stay in the DOM so save/load always covers all areas.

- `notice` controls **공지사항** write/edit/delete (was admin-only; now `userCan('notice')`, so admin/`all`/notice-holders) — **client-gated only**: the Firestore rule still allows any employee to update `staffPosts/notice` because the per-view 조회수 save (`viewPost`→`commitBoardChange`) is also an `update`, and server-gating it would make read-only viewers hit the save-failure alert.
- `cert` controls the 회사운영 **규격서/인증서** segment/board (label renamed from 인증서; key stays `cert`) (`staffPosts/cert`, third `#notice-segment` button via `setNoticeSegment('cert')` — which switches `currentBoard` to `'cert'`; the 공지사항/주요회사운영 segments belong to board `'notice'`): write/edit/delete = `userCan('cert')`, read = all employees, attachments enabled (file-board like notice) — **client-gated only**, same 조회수-update reason as `notice`.
- `report` controls the 직원게시판 **업무보고(본사)** segment (`staff-report` board): the segment button is hidden and access blocked for users without it.
- `asAssignee` controls the A/S 처리결과 **출동 담당자 관리**(추가/삭제 of `appConfig/asAssignees`) — gated in UI via `asrCanManageAssignee()` (button visibility + `openAssigneeManager`/`addAssignee`/`removeAssignee`) and enforced in the Firestore rule for `appConfig/asAssignees`.
- `leaveSchedule` controls the 직원게시판 **연월차일정** calendar's **edit/delete** only — **adding** events is open to all employees.
- The **회원관리** nav (`switchBoard('member-management')`) is **admin-only** — the nav link is `display:none` by default and revealed only for admins in `checkStaffAccess`; `switchBoard` also blocks non-admins.

## 연월차일정 캘린더 (leave calendar)

The monthly-calendar engine is shared between 주요회사운영 (`companyCalendar`, 회사운영 tab) and 연월차일정 (`leaveSchedule`, 직원게시판 tab) via a `currentCalKind` ('company'|'leave') + `CAL_KINDS` config (`collection`/`viewId`/`canAdd`/`canManage`); the shared `loadAndRenderCalendar`/`renderCalendar`/`renderDayModal`/`addCompanyEvent`/`editCompanyEvent`/`saveCompanyEventEdit`/`deleteCompanyEvent` read `calCfg()`. `staff-leave` is a `switchBoard` board type that shows `#leave-calendar-view` (like `as-result` shows the asr view).

- **월별 결재란**: the leave calendar has a 결재란 in the title row's right end (`leaveApprovalHtml()`, rendered by `renderCalendar` when `currentCalKind==='leave'`, A/S 결재선 스타일 재사용, nav-button height; wraps below the nav at ≤480px) with **two cells** driven by `LEAVE_APPR_STAGES`: 본사관리자=`hq` (perm `leaveApprove`) and 공장관리자=`factory` (perm `leaveApproveFactory`). Per displayed month, an unapproved cell shows a "미결재" button only for that stage's perm holders or admins (`leaveApprPermFor(stage)` via `asrPerm`); `approveLeaveMonth(stage)` confirm-stamps 직책+이름+날짜 into `appConfig/leaveApprovals.months['YYYY-MM'][stage]` (`set merge`, loaded once via `loadLeaveApprovals()`; a legacy flat stamp `months[ym]={title,name,date}` is read as the `hq` stamp by `leaveStampsOf()`).
- **A stamp locks that month per employee group**: the `hq` stamp locks 본사 임직원 (`users.empGroup==='hq'`, group from `leaveUserGroup()`), the `factory` stamp locks all other employees — the locked group can't add and even its `leaveSchedule`/`all` holders can't edit/delete; only 결재자(either stage perm)/admins bypass (`CAL_KINDS.leave.canAdd/canManage` take a date arg and check `leaveMonthLockedForGroup()`; period operations crossing a locked month are blocked via `leaveLockedInRange()` guards in `addCompanyEvent`/`saveCompanyEventEdit`/`deleteCompanyEvent`; the day modal shows a `.day-lock-note` listing stamped stages). **The lock is enforced on BOTH the editor's group and the event owner's group** (2026-07-14): leave docs store `ownerGroup`('hq'|'factory'|null) stamped at add/edit from the registrant's group when self-registered (admin 대리 등록 with a changed author = null); `leaveDocLockedInRange(ev,…)` blocks edit/delete/edit-form-entry (and hides the day-modal 수정/삭제 buttons via `calEvLocked`) when the owner group's stamp exists — so e.g. an hq `leaveSchedule` holder can't touch a factory member's event after the factory stamp. Docs with `ownerGroup` null/missing (legacy·대리 등록) go through `leaveDocGroupOf(ev)` (2026-07-16): if the doc is **the logged-in user's own** (name match via `hrLeaveIsOwn` — same matching as 연월차일정현황) the owner group is inferred as the user's group (so a factory member's own legacy event is NOT locked by an hq-only stamp); other people's unknown docs stay conservatively locked by EITHER stamp (regular employees can't read `users` so a name can't be resolved to a group). Shared iterator `leaveLockedInRangeBy(start,end,group)` (group null = any stamp); `leaveLockMsg(ym, group?)` names the locking stage (lock-alert sites pass `leaveDocGroupOf(ev)`). The `leaveSchedule` Firestore rule's update/delete also allows `permFor('leaveApprove')`/`permFor('leaveApproveFactory')` so 결재자 without `leaveSchedule` can correct locked months — the month-lock itself is client-gated only.
- **The leave calendar's add/edit forms are a simplified sheet** (no title/time/memo): 휴가기간 + 연차일수(1일/0.5일) + 종류(연차/휴가/병가/경조사/기타) selects; the title is auto-built by `buildLeaveTitle()` as `"직급 이름(연차N)"` + ` / 종류` when 종류≠연차 (author from `currentAuthorLabel()`, read-only; edit keeps the original author via `leaveInfoOf()`, which also parses legacy titles). When the period spans more than one day (start≠end) the 연차일수 select is disabled (`updateLeaveDaysBadge()` add / `leaveEditSyncDays()` edit, wired to the date inputs' onchange) and the title shows the period instead — `"직급 이름(7.8~7.10) / 휴가"` (`calFmtShort()` M.D format, `leaveDays` saved as null).
- **Admins and 결재권자 (`calCanEditLeaveAuthor()` = `calIsAdmin()` OR `leaveApprPerm()`) can change the author** — for them the 작성자 field renders as a text input (`.day-leave-author-input`) in both add and edit forms (대리 등록/정정); others get the read-only auto-filled label. `CAL_KINDS.leave.canManage` also passes 결재권자 in un-stamped months (not just `leaveSchedule` holders) so they can enter the edit form — matches the Firestore rule, which already allows their update/delete.
- Multi-day range adds stamp `periodStart`/`periodEnd` on each day's doc, shown as small grey text above the title in the day modal. Leave docs additionally store `author`/`leaveDays`/`leaveType`; the shared `#day-add-form` toggles `#day-add-company-fields` vs `#day-add-leave-fields` per `currentCalKind`, and the day-modal list hides the time column for leave.

## Staff Page Modules (`hanaro/staff/staff.html`)

This single large file (~15.5k lines, 9 inline `<script>` blocks) holds all employee tools:
- **운영관리 통합 셸** (`#ops-modal`, z 2600): the nav no longer has separate 자재관리/재고현황/일정관리 links — one **운영관리** link opens a full-screen shell with a left sidebar (mobile: title row + horizontal chip row) of 인사관리/일정관리/자재관리/재고현황. **인사관리** is a segment panel (인사기록부/재직증명서 발급/연월차일정현황/급여명세서): 인사기록부 is real (`hrRecords/{uid}` — 임직원은 본인 기록 읽기전용, `hrCanManage()`(admin or 명시적 `permissions.hr`, 'all' 미포함) 데이터관리자·관리자는 대상 임직원 select 후 작성·수정·삭제; `hr*` functions in the ops shell script block). **연월차일정현황 is real too** (`hrOpenLeave`/`hrLeaveLoadAndRender`): 직원게시판 연월차일정(`leaveSchedule`)을 산정 기간으로 조회해 본인(데이터관리자는 대상 선택) 사용 내역을 집계 — 발생 연차는 근로기준법 제60조(`hrLeaveEntitlement`: 1년 미만 월 1일·최대 11일 / 15일 / 3년 이상 2년당 +1·최대 25일, 입사일은 hrRecords.joinDate), **사용 일수는 해당 월의 결재 도장(`appConfig/leaveApprovals`, 그룹별 hq/factory — `hrLeaveStampOf`)이 있어야 반영**, 미서명 달은 '결재 대기'로 비차감 표시; 연차·휴가만 차감(병가·경조사·기타 비차감), 문서 매칭은 author/createdBy 이름 기준(`hrLeaveIsOwn`). **연월차 어시스턴트**(규칙 기반 Q&A 챗, 외부 API 없음)는 **기준 정보 카드 내부 우측 상단 💬 아이콘**(`#hr-lv-fab` — `hrLeaveLoadAndRender`가 카드 HTML에 렌더, 카드 `position:relative`+아이콘 `absolute`)으로 여는 **별도 챗 모달 시트**(`#hr-lv-modal`, z 3000 — 운영관리 셸 z2600 위; 닫기=알약 버튼·백드롭 클릭, 세그먼트 이탈·`closeOpsModal` 시 자동 닫힘): `hrLeaveLoadAndRender`가 화면 수치와 동일한 산정 결과를 `hrLeaveCtx`에 저장하고, `hrLeaveAnswer(q)`가 키워드 매칭으로 잔여·발생·사용·결재·다음 발생일(`hrLeaveNextAccrual` — 1년 미만 다음 개근월 +1일/1주년 15일, 1년 이상 다음 입사기념일 재발생)·근로기준법 규정·**미사용 연차 수당/이월/소멸**(제60조7항 소멸→수당 전환, 제61조 촉진 시 면제, 이월=노사 합의 시 가능 — '사용' 매처보다 앞에 배치해 '미사용' 질문 오분류 방지) 질문에 답변(예시 질문 칩 4종 + 입력줄, 사용자 입력은 `schedEsc`; 대상 변경 시 ctx 초기화·재산정). **관리자·데이터관리자(`hrCanManage()`) 계정은 어시스턴트 이용 불가** — 어떤 질문이든 "관리자/데이터관리자 계정은 이용할 수 없습니다" 안내만 반환(임직원 본인용 기능, 관리자는 위 '연월차 현황 조회' select로 직접 확인). **급여명세서 is real** (`hrOpenPay`/`payExcelPicked`/`payGenerateAndUpload`): 데이터관리자가 급여 엑셀(.xlsx, 제목 'YYYY年 M月'·'지급일' 행·'소속/성명' 블록 반복 양식)을 업로드하면 SheetJS로 블록별 파싱 → 성명(동명이인은 직위) 매칭(미매칭은 미리보기에서 수동 선택/제외) → html2canvas+jsPDF(jsDelivr, 지연 로드)로 명세서 PDF 생성 → Storage `pay-slips/{ym}_{uid}.pdf` 업로드 → `paySlips/{uid}.months[ym]` set merge. 열람은 본인 목록(`payRenderMyList`)만. **급여명세서 이의제기**: 내 명세서 각 행의 '이의제기' 버튼 → 폼(`#pay-inq-form-card`) 접수 → `payInquiries` 컬렉션 → `payCanManage()` 보유자의 '이의제기 문의함'(`#ops-hr-pay-inbox`, 미답변 N건 배지)에서 답변 등록(status `answered`)·삭제, 문의자는 '내 이의제기 문의' 카드에서 상태·답변 확인+미답변 취소(`payInq*` functions; 접근 매트릭스는 컬렉션 표). 재직증명서 발급 segment is still a static mockup (`opsHrMock`). `openOpsModal()` reparents the three existing tool modals into `#ops-tool-host` (once, they stay there); scoped CSS `.ops-tool-host .…-modal { position:static }` turns them into fill-height panels and hides their own 닫기/backdrop. `opsSelect(key)` closes the previous tool via its own close fn (listener/table teardown) then calls the tool's open fn — tool internals unchanged; it re-locks `body{overflow:hidden}` after each switch because tool close fns unlock it. Tools' auxiliary overlays (색상표 z4200, ctx menus z4000, staff confirm z100000) sit above the shell. Last-selected tool is remembered per page load (`opsCurrent`; default 일정관리).
- **재고현황 / 일정관리** share one in-house spreadsheet engine (`sheet*` functions: `sheetRenderTable`, `sheetBind`, `sheetEditCell` double-click edit, drag-select via `schedSel` + Shift+click range extend + edge auto-scroll while dragging, right-click `#sheet-ctxmenu`, merge/align, undo/redo `sheetPushUndo`). Model: `{columns, grid, merges, aligns, fills, colWidths, rowHeights, ...}` — `colWidths`/`rowHeights` are drag-resized via `.sheet-colgrip`(header right edge; first resize snapshots all widths then switches to `table-layout:fixed`)/`.sheet-rowgrip`(cell bottom edge); header cells drag-select whole columns (`sheetSelectCols`, click alone still renames). `fills`(per-cell background, 16-color palette `#sheet-fillpal` from the ctx menu) is 일정관리-only in effect (재고현황's `invApplyGroupColors` overwrites backgrounds — it keeps per-group `rowColors`). **Arrays persisted to Firestore must have no holes** — `sheetEnsureSizes` null-fills them (undefined in arrays = `Transaction.set` rejects; caused a save-failure bug). 2D fields (`grid`/`aligns`/`fills`) are stored as JSON strings (no nested arrays in Firestore).
- **자재관리** = **'월재고' 엑셀 시트 탭 8종 전용** (2026-08-05): 모달 상단 `.mat-tabs`(`matRenderTabs`)의 탭이 곧 `MAT_SHEETS`이고, 전부 **재고현황과 같은 시트 엔진**을 쓰는 매트릭스 표다(`matTabSelect(key)`로 전환, 본문은 항상 `#matsheet-panel`).
  - ⚠️ **옛 '자재목록' 탭(Tabulator + `materials` 컬렉션)은 사용자 요청으로 제거됐다** — `openMaterialsModal`이 `renderRibbonButtons`/`initMaterialsGrid`/`startMaterialsListener`를 더 이상 호출하지 않고, 엑셀 메뉴바·리본(`#excel-menubar`·`#excel-ribbon`)과 `#materials-grid`를 계속 `display:none`으로 둔다. **구현부(약 1,700줄: Tabulator 초기화·리본·우클릭 메뉴·앱 레벨 undo/redo·CSV/JSON/Excel 입출력·`materialsKeyboardHandler`)와 `materials` 컬렉션 데이터는 지우지 않고 그대로 남겼다** — 되살리려면 `openMaterialsModal`의 그 세 줄과 `matRenderTabs`의 탭 목록만 복구하면 된다. 현재 이 코드는 전부 도달 불가(dead)이니 staff.html을 읽을 때 살아있는 경로로 착각하지 말 것.
  - **시트 탭 8종**(`MAT_SHEETS`: `prod`/`vendor`/`inject`/`brow`/`neworder`/`pipe`/`matsum`/`alllist`)은 사용자의 '월재고' 엑셀(표시 시트 8개 — 숨김 550개는 지난 납품일정 아카이브)의 헤더 행에서 그대로 뽑은 열 구성이다. 대부분 **품목 × 1~31일 매트릭스 + 집계열**이라 Tabulator의 '행=문서, 고정 필드' 모델로는 표현이 안 돼 시트 엔진을 재사용했다. 시트 1개 = **`materialSheets/{key}` 문서 1개**, 저장 형식·낙관적 잠금(`matSheetLoadedAt`)은 `inventory`와 동일(2D 필드는 JSON 문자열). 저장 전 `MAT_SHEET_MAX_BYTES`(900KB)로 문서 1MB 한도를 막는다 — 실측 최대는 총생산출고품목 66KB라 여유가 크다.
  - **엑셀 업로드**(`matSheetImportExcel`→`matSheetExcelPicked`)는 매달 같은 양식 파일을 올려 해당 탭을 채우는 용도. 두 가지 함정을 처리한다: ① 헤더 행이 1행이 아니다(`'10월|사출재고|출고내역'` 같은 제목 행이 위에 있음) → `matDetectHeaderRow`가 **양식 열 이름과 가장 많이 겹치는 행**을 헤더로 고른다(8시트 전부 실측 통과). ② 시트 이름을 공백 무시로만 매칭하면 **숨김 구버전 시트를 집는다** — 실제 파일에 `'브로우 및 상판재고'`(숨김)와 `'브로우및상판재고 '`(표시)가 함께 있다 → `matFindSheetName`이 완전일치 → 표시 시트 → 그 외 순으로 찾는다(`matVisibleSheetNames`는 `wb.Workbook.Sheets[i].Hidden` 사용). 병합(`ws['!merges']`)도 헤더 행 기준으로 좌표 보정해 가져온다.
  - **초기 데이터**: 저장된 `materialSheets/{key}` 문서가 없는 탭은 `hanaro/staff/data/materialSheets-2026-08.json`(2026.08 엑셀에서 추출, 161KB)을 채워 보여준다 — `matLoadSeed()`가 **시트 탭을 처음 열 때만 fetch**(staff.html에 인라인하면 전 직원이 매 방문 160KB를 더 받는다), `matSeedCache`에 보관. 한 번 저장하면 문서가 생겨 이후로는 시드를 쓰지 않는다. 안내줄(`matSheetNotice`)에 '초기 데이터 — 저장을 눌러야 반영됩니다'를 표시. 시드 JSON의 키는 `MAT_SHEETS`의 `key`와 1:1이어야 한다.
  - **읽기 실패는 alert하지 않는다** — 규칙 미게시/네트워크 오류 시 탭을 누를 때마다 팝업이 뜨면 못 쓴다. `matSheetNotice`에 경고를 표시하고 초기 데이터로 진행(저장 실패는 그대로 alert — 이쪽은 사용자가 조치할 수 있다).
  - **열 폭·행 높이는 코드가 고정한다** — `colWidths`가 비어 있으면 `sheetRenderTable`이 `table-layout:auto`로 두어 브라우저가 내용 길이대로 열을 잡고, 1~31일 칸이 제각각 벌어져 표가 들쭉날쭉해진다(사용자 보고). `matDefaultColWidths`가 날짜 열 44px / 첫 열 190px / 규격 96px / 집계·비고 78px(날짜 열이 없는 시트는 118px)을 넣고, `matApplyDefaultColWidths`는 **사용자가 직접 조절해 저장한 값이 하나라도 있으면 건드리지 않는다**. 행 높이는 CSS(`#matsheet-panel … td { height:28px }`)로 균일화 — 추가한 행도 같은 높이가 되고 문서에 `rowHeights`를 쌓지 않는다. **공용 `.schedule-table td { min-width:90px }`가 진짜 원인**이었다: `th`에는 인라인 너비가 붙지만 `td`에는 안 붙어 44px 지정이 무시됐다 → `#matsheet-panel` 스코프로 `min-width:0`·`letter-spacing:normal`을 덮어쓴다(id 선택자라 순서와 무관하게 우선). 좁아져 잘린 값은 `matSheetRender`가 `title` 툴팁으로 보여준다.
  - 쓰기 권한은 `userCan('materials')`로 자재목록과 동일(`matSheetCanEdit`), 읽기는 임직원 전체. **`sheetModalOpen()`에 자재관리 모달이 포함되지만 `matTab !== 'list'`일 때만** — 안 그러면 자재목록 탭에서 직전에 열었던 재고현황/일정관리 모델에 단축키가 걸린다. 같은 이유로 `materialsKeyboardHandler`도 시트 탭에서 즉시 반환한다(Ctrl+Z 이중 처리 방지).
- **활동사진첩**: photos carry an optional `title`. The viewer groups photos by identical title (next/prev cycles within the group), supports mobile swipe. Upload assigns one common title to the batch; grid titles are double-click editable by owner/admin only. **그룹에 사진 추가 (2026-07-30)**: **뷰어 하단**의 `➕ 사진 추가` 알약 버튼(`#photo-add-to-group-btn` → `addPhotoToCurrentGroup`)이 `openPhotoUpload(forcedTitle)`을 현재 사진의 제목으로 호출해 업로드 모달의 제목 입력을 readonly로 잠근 채 연다(오타로 그룹이 갈라지는 것 방지; `closePhotoUpload`가 잠금·모달 제목을 원복). 업로드 모달 z2000 < 뷰어 z3000이라 **뷰어를 먼저 닫아야** 모달이 보인다. **썸네일 위에는 버튼을 얹지 않는다**(사진을 가려 한 번 되돌린 설계). 빈 칸 플레이스홀더는 `userCan('photos')` 보유자에게 `.photo-empty-slot.addable`로 렌더되어 클릭 시 빈 제목 업로드(새 그룹). 장수 배지(`.photo-count-badge`)는 **좌상단**. 뷰어의 사진 삭제는 관리자 전용이 아니라 **관리자 또는 본인 업로드분**(`canDeletePhoto()` — 그 한 장의 소유만 확인; Firestore delete 규칙과 동일. 제목 수정·순서 변경은 여러 사람 문서를 함께 건드리므로 여전히 `canEditPhotoTitle`로 그룹 전원 소유/관리자 요구).
  - **그룹 키는 `photoTitleKey()`(NFC 정규화 + trim)** — `trim()`만으로 비교하면 한글 완성형(NFC)/조합형(NFD)이 화면상 동일한데 문자열로 달라(macOS 파일명·Finder 복사 텍스트가 NFD) 같은 제목이 별개 그룹으로 갈라져 **똑같아 보이는 카드가 2장** 생긴다. 비교(`photoGroupOf`·그리드 중복 제거)와 저장(업로드 제목·`savePhotoTitle`) 양쪽에 적용 — 비교 시점 정규화라 기존 저장분도 마이그레이션 없이 합쳐진다.
  - **이미지 최적화 (2026-08-03)** — 예전엔 `storageRef.put(file)`로 **원본을 그대로** 올려(모달의 "자동 압축" 안내와 달리 `compressImage`는 Storage 실패 시 base64 폴백에만 쓰였다) 목록은 카드 수만큼, 뷰어는 넘길 때마다 수 MB 원본을 내려받아 느렸다. 이제 업로드 전에 `photoProcessImage(file)`이 **한 번의 디코딩으로 두 벌**을 만든다 — 본문용(`PHOTO_MAX_DIM` 1600px / `PHOTO_MAX_KB` 800KB 이하)과 목록 썸네일(`PHOTO_THUMB_DIM` 480px / `PHOTO_THUMB_KB` 80KB 이하). `photoCanvasBlob()`이 목표 용량 이하가 될 때까지 JPEG 품질을 0.85→0.45로 낮추며 `toBlob`; 투명 PNG는 흰 배경을 먼저 깔고(검은 배경 방지), **GIF는 본문 원본 유지**(압축하면 애니메이션이 죽음, 썸네일만 정지 생성). 썸네일 URL은 문서의 `thumbUrl`(+실제 업로드 크기 `width`/`height`)에 저장하고 그리드 `<img>`가 `thumbUrl || url`을 쓴다. **Storage 경로는 `activity-photos/` 평면 유지**(`{timestamp}_thumb_{base}.jpg`) — 규칙이 `match /activity-photos/{fileName}` 단일 세그먼트라 하위 폴더는 차단된다. 압축본이 원본보다 크면 원본을 올리고, 썸네일 업로드 실패는 무시(원본 폴백). Firestore 저장 실패 시 본문·썸네일 둘 다 보상 삭제하고, `deletePhoto`도 `thumbUrl`을 함께 지운다.
  - **뷰어 넘기기 선로딩** — `photoImgCache`(url→`Image`, 40개 상한)에 현재 위치 ±1·±2를 미리 받아둬(`photoPreloadNeighbors`) 다음/이전이 즉시 전환된다. 미리 받힌 게 없으면 **썸네일을 먼저 띄우고**(썸네일 없는 옛 사진은 직전 사진을 `opacity:.35`로) `#photo-viewer-spinner`를 보인 뒤 원본 도착 시 교체 — 늦게 도착한 로드가 이미 넘어간 사진을 덮지 않도록 `viewerRenderToken` 세대 비교(닫을 때도 증가).
  - **기존 사진 일괄 최적화** — 썸네일 없이 원본만 있는 옛 사진용 `optimizeExistingPhotos()`(`#photo-optimize-btn`, 헤더, **관리자 전용**·대상 있을 때만 노출, `updatePhotoOptimizeBtn`이 `renderPhotoGrid`에서 갱신). 원본을 `fetch`(Storage CORS 허용 도메인에서만 동작 — `cors.json`) → 압축·썸네일 생성 → 업로드 → 문서 `url`/`thumbUrl` 교체 → **문서 갱신 성공 후에만** 옛 원본 Storage 삭제. 본문 교체는 20% 이상 작아질 때만, base64 레거시(`https` 아님)는 대상에서 제외. 비관리자가 남의 사진을 못 고치는 Firestore update 규칙과 맞물린 관리자 게이트다.
  - **업로드 대기 목록은 중복 선택을 거른다** — `handlePhotoSelect`가 이름+크기+수정시각 키로 이미 담긴 파일을 제외(예전엔 무조건 append라 파일창을 다시 열어 같은 사진을 재선택하면 두 번 업로드돼 같은 사진이 그룹에 중복 등록). 매번 `photo-file-input.value`를 비워 지웠던 파일 재선택도 동작. 미리보기 카드는 선택 순서대로 먼저 만들고 이미지만 나중에 채운다(예전엔 `FileReader.onload` 안에서 append해 읽기 완료 순서로 뒤섞여 ✕의 index가 화면과 어긋나 다른 사진이 지워졌다).
- **게시판 글쓰기 리치 에디터**: the shared write form (`#write-form`) uses a `contenteditable` editor (`#write-content`, `.rte-editor`) + toolbar (`#write-rte-toolbar`), not a textarea. Bold/italic/underline/strike/list/align run via `document.execCommand`; font color/highlight via `foreColor`/`hiliteColor`; **pt font size wraps the selection (or caret, via a ZWSP span) in `<span style="font-size:Npt">`** since execCommand fontSize only supports 1–7. Post body is stored as **HTML, sanitized with `rteSanitize()` (DOMPurify allowlist) on save and on render** — legacy plain-text posts are auto-detected and rendered with `escape + nl2br`. Helpers: `rteSanitize`, `rteToText`, `rteLinkify` (view-time only: turns `https://`/`www.` in post-render text nodes into `target=_blank` links — the sanitizer allowlist deliberately does NOT allow `a`, stored content unchanged), `initRteToolbar` (tracks the last selection range so the OS color picker doesn't lose it). Applies to ALL boards sharing the form. **본문 이미지 (2026-07-20)**: paste/drag&drop/toolbar 🖼️(`#rte-img-btn`) insert images at the caret as data: URLs via `rteInsertImageFiles` (paste is intercepted because Safari otherwise inserts unreadable `webkit-fake-url`; svg excluded — Storage rule blocks it); on save `submitPost` (now async) calls `rteUploadBodyImages` to upload data:/blob: body images to Storage `as-result-files/` (via `uploadFilesToStorage`, 10MB/image) and swap src to the download URL BEFORE sanitizing — never let base64 into the Firestore doc (1MB limit). The sanitizer allows `img` with `src` matching `^https://` only (`ALLOWED_URI_REGEXP`); an image-only body counts as non-empty (`hasBodyImage`). The 작성자 field (`#write-author`) is auto-filled read-only with `currentAuthorLabel()` (직급/직책 + 이름 — same helper the leave calendar uses); don't make it user-editable.
- **게시판 검색** (`searchPosts`): there is ONE shared search element (`.search-group#board-search`, a 제목/내용 select + input + magnifier button). `placeBoardSearch()` **relocates it to the right end of the active segment menu** (`.notice-segment` row) — `#notice-segment` (공지사항), `#staff-segment` (직원게시판), `#asresult-segment` (A/S) — styled as a `.seg-search` pill (알약형) with the select hidden so it searches title **and** content (`searchType='all'`). 제안활동 has no segment menu, so it uses a right-aligned bar `#suggestion-search-bar` at the top of `#notice-post-view`. The search is hidden on 캘린더/활동사진첩/회원관리. `placeBoardSearch()` is called from `switchBoard`, `setNoticeSegment`, `setAsrSegment` and **clears the query on every move**; the parked `.board-footer` home is `display:none`. **A/S 처리결과 uses a separate data path** (`asrData`/`renderAsResult`, not `allPosts`): `searchPosts` delegates to `searchAsr()`, which sets `asrSearchQuery`; `renderAsResult` filters rows by 제목·작성자·구분·내용·담당자.
- **A/S 처리결과** is `asPosts`-backed with assignee management (`appConfig/asAssignees`), replies, and completion handling. Kept live via an `onSnapshot` listener on `asPosts/posts` (`startAsrRealtime`, started from `startStaffListeners`) so new AS.html applications and processing updates appear without re-clicking the tab; while the detail modal (`asr-modal`) is open, incoming changes are buffered in `asrPendingPosts` and applied on `closeAsrModal` to avoid `asrCurrentIdx` desync. (AS.html's applicant-side list is still one-shot `.get()`.) **완료(done)** 건 상세에는 4단계 **결재선**(`asrApprovalHtml`): 출동담당자(자동확인=assignee/completedBy·completedDate) → 담당자 → 관리자 → 대표이사. The 3 manual stages stamp `apprStaff`/`apprAuth`/`apprCeo` = `{title,name,date}` on the post. Each pending cell renders the "미결재" text as a **button** (`approveAsr(stage)`) only for the user who may stamp the **next sequential** stage (담당자→관리자→대표이사); clicking just **confirms** (no manual input) and auto-stamps the approver's **직책(=`users.position`) + 이름 + 날짜** (대표이사 stage falls back to 직책 "대표이사" if `position` is unset). Stage permission is checked via `asrPerm(key)` — `users.permissions[key]` holders **plus admins (`isAdmin`/`admin` auto-pass); NO `all` override**: 담당자=`asApprStaff`, 관리자=`asApprove`, 대표=`asApproveCeo`. Approval data lives in `asPosts/posts` (client-gated like the rest of AS — no extra Firestore rule). The detail also has a **구매이력 확인** card (above 답글/메모) — memo (`purchaseNote = {text,by,date}`) writable by anyone holding any 결재 permission (`asrCanPurchase()`); **this memo is also shown to the applicant on the public AS.html detail modal** (`#modal-as-status`, 구매이력 확인 card) whenever `purchaseNote.text` exists. The **처리내용** card is renamed **A/S 출동담당자(처리 내용)** and is writable/completable by **the assigned 출동담당자 or an admin** (`asrCanReport()` = `asrIsDispatcher()` — name match between the logged-in user and `p.assignee` — OR `asrIsAdmin()`; `completeAsr` enforces it). **담당자 지정(assignee select)** is gated by `asrCanAssign()` = `asrCanEdit()` (asResult) **OR `asrIsRegisteredDispatcher()`** (logged-in user's name matches an entry in the 출동담당자 목록 `asrAssignees`), so a registered 출동담당자 can self-assign **without** asResult and thereby unlock their own 처리내용 input (`assignAsr` enforces `asrCanAssign()`; asPosts write is `isAuthenticated`, no rule change). Editing the master 출동담당자 목록 still needs `asAssignee` (`asrCanManageAssignee()`). 처리내용 supports **image attachments** (`p.asReportFiles = [{name,url,type,size}]`, Storage URLs only via the global `uploadFilesToStorage`): the dispatcher picks images (pending `File`s in `asrReportSelected`, previewed as removable thumbnails), and `completeAsr` uploads them on save+complete; saved images render as thumbnails via `asrReportImagesHtml()` (reuses `.asr-thumb`) in all three 처리내용 states and are listed (filenames) in the Excel export. 답글/메모 stay gated by `asrCanEdit()` (`asResult`); 담당자 지정 uses `asrCanAssign()` (see above). A **완료(done)** item's detail also shows a **🗑️ 게시물 삭제** button (`.asr-btn.danger`) visible **only to web/server admins** (`asrIsAdmin()`); `deleteAsrDone()` splices the post out of the shared `asPosts` array and saves — so it **also disappears from the public AS.html applicant list** (full delete, with confirm + rollback on save failure; no Firestore-rule change, admin gate is client-side like the rest of AS). The detail modal of a **완료(done)** item also shows a `#asr-export-btn` that calls `exportAsrDetailExcel()` — builds an AOA (구분·제목·작성자·신청일·담당자·신청내용·첨부·답글·처리내용·완료자/일) and `XLSX.writeFile`s `A_S신청상세_<제목>_<완료일>.xlsx` via the already-loaded SheetJS.

