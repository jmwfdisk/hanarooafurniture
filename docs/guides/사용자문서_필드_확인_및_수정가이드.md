# 사용자 문서 필드 확인 및 수정 가이드

## 📋 현재 사용자 문서 구조

**문서 ID**: `jrSeEHFpXiPJKymdRuYDLhhjHxk1`

**현재 필드:**
- ✅ `uid`: "jrSeEHFpXiPJKymdRuYDLhhjHxk1" (문자열)
- ✅ `email`: "jmwfdisk@hotmail.com" (문자열)
- ✅ `isAdmin`: true (불리언)
- ⚠️ `displayName`: "정민우" (문자열) - **`name` 필드가 필요함**

---

## ❌ 누락된 필드

코드에서 필요한 필드 중 다음이 누락되어 있습니다:

1. **`username`** (문자열) - 사용자명
2. **`name`** (문자열) - 이름 (현재 `displayName`이 있지만 `name`이 필요)
3. **`phone`** (문자열) - 전화번호 (선택사항이지만 있으면 좋음)
4. **`userType`** (문자열) - 사용자 유형 ("general" 또는 "employee")
5. **`status`** (문자열) - 승인 상태 ("pending", "approved", "rejected")
6. **`createdAt`** (문자열) - 생성 일시

---

## ✅ 필수 필드 추가 방법

### 방법 1: Firebase Console에서 수동 추가

1. **Firebase Console > Firestore Database > 데이터 탭**
   - `users` 컬렉션 선택
   - 문서 ID `jrSeEHFpXiPJKymdRuYDLhhjHxk1` 선택

2. **"+ 필드 추가" 버튼 클릭**

3. **다음 필드들을 추가:**

   #### 필수 필드:
   ```
   필드 이름: username
   유형: string
   값: "사용자명" (예: "jmwfdisk" 또는 원하는 사용자명)
   ```

   ```
   필드 이름: name
   유형: string
   값: "정민우" (displayName과 동일한 값)
   ```

   ```
   필드 이름: userType
   유형: string
   값: "general" (일반 사용자) 또는 "employee" (임직원)
   ```

   ```
   필드 이름: status
   유형: string
   값: "approved" (로그인 가능하도록)
   ```

   ```
   필드 이름: createdAt
   유형: string
   값: "2025-01-XX..." (현재 날짜/시간, 예: "2025-01-15T09:00:00.000Z")
   ```

   #### 선택 필드:
   ```
   필드 이름: phone
   유형: string
   값: "전화번호" (선택사항)
   ```

4. **저장**

---

### 방법 2: displayName을 name으로 변경

현재 `displayName` 필드가 있지만, 코드에서는 `name` 필드를 사용합니다.

**옵션 A: displayName 삭제하고 name 추가**
1. `displayName` 필드 삭제
2. `name` 필드 추가 (값: "정민우")

**옵션 B: displayName 유지하고 name도 추가**
1. `name` 필드 추가 (값: "정민우")
2. `displayName`은 그대로 유지

---

## 📝 완성된 문서 구조 예시

**문서 ID**: `jrSeEHFpXiPJKymdRuYDLhhjHxk1`

**필드 목록:**
```
uid: "jrSeEHFpXiPJKymdRuYDLhhjHxk1" (string)
email: "jmwfdisk@hotmail.com" (string)
isAdmin: true (boolean)
name: "정민우" (string) ← 추가 필요
username: "jmwfdisk" (string) ← 추가 필요
userType: "general" (string) ← 추가 필요
status: "approved" (string) ← 추가 필요
createdAt: "2025-01-15T09:00:00.000Z" (string) ← 추가 필요
phone: "" (string) ← 선택사항
```

---

## 🔍 코드에서 사용하는 필드 확인

### 로그인 시 확인하는 필드 (auth.js:93):
```javascript
if (userData.status === 'approved' || userData.isAdmin) {
    // 로그인 허용
}
```

**현재 상태:**
- ✅ `isAdmin: true` → 로그인 가능
- ⚠️ `status` 필드가 없지만 `isAdmin: true`이므로 로그인은 가능해야 함

### 세션 스토리지에 저장하는 필드 (auth.js:95-99):
```javascript
sessionStorage.setItem("loggedInUser", JSON.stringify({
    uid: user.uid,
    email: user.email,
    ...userData  // 모든 userData 필드 포함
}));
```

**필요한 필드:**
- `uid` ✅
- `email` ✅
- `name` ⚠️ (없음, `displayName`만 있음)
- `username` ❌ (없음)
- `userType` ❌ (없음)
- `status` ❌ (없음, 하지만 `isAdmin: true`이므로 문제 없음)
- `isAdmin` ✅

---

## ⚠️ 중요 사항

### 1. status 필드

**현재 상황:**
- `status` 필드가 없지만 `isAdmin: true`이므로 로그인은 가능합니다
- 코드: `if (userData.status === 'approved' || userData.isAdmin)`

**권장:**
- `status: "approved"` 필드를 추가하는 것이 좋습니다
- 일관성을 위해 모든 사용자 문서에 `status` 필드가 있는 것이 좋습니다

### 2. name vs displayName

**현재:**
- `displayName: "정민우"` 있음

**필요:**
- `name: "정민우"` 필요

**해결:**
- `displayName`을 `name`으로 변경하거나
- 둘 다 유지 (코드는 `name`을 사용)

---

## ✅ 빠른 수정 방법

### 최소한 추가해야 할 필드:

1. **name** (문자열)
   - 값: "정민우"

2. **username** (문자열)
   - 값: 원하는 사용자명 (예: "jmwfdisk")

3. **userType** (문자열)
   - 값: "general" 또는 "employee"

4. **status** (문자열)
   - 값: "approved"

5. **createdAt** (문자열)
   - 값: 현재 날짜/시간 (예: "2025-01-15T09:00:00.000Z")

---

## 🎯 권장 작업 순서

1. **즉시 추가:**
   - `name`: "정민우"
   - `status`: "approved"
   - `userType`: "general" (또는 "employee" if 임직원)

2. **나중에 추가:**
   - `username`: 사용자명
   - `createdAt`: 생성 일시
   - `phone`: 전화번호 (선택사항)

3. **선택사항:**
   - `displayName` 필드 삭제 (중복이므로)

---

## 📊 필드 비교표

| 필드 | 현재 상태 | 필요 여부 | 우선순위 |
|------|----------|----------|----------|
| `uid` | ✅ 있음 | 필수 | - |
| `email` | ✅ 있음 | 필수 | - |
| `isAdmin` | ✅ 있음 | 필수 | - |
| `name` | ❌ 없음 | 필수 | 높음 |
| `username` | ❌ 없음 | 권장 | 중간 |
| `userType` | ❌ 없음 | 권장 | 중간 |
| `status` | ❌ 없음 | 권장 | 높음 |
| `createdAt` | ❌ 없음 | 권장 | 낮음 |
| `phone` | ❌ 없음 | 선택 | 낮음 |
| `displayName` | ⚠️ 있음 | 불필요 | - |

---

## 💡 핵심 정리

**현재 문서는 로그인은 가능하지만 (`isAdmin: true`), 완전한 사용자 정보를 위해서는 다음 필드들을 추가하는 것이 좋습니다:**

1. `name`: "정민우" (필수)
2. `status`: "approved" (권장)
3. `userType`: "general" 또는 "employee" (권장)
4. `username`: 사용자명 (권장)
5. `createdAt`: 생성 일시 (권장)

이 필드들을 추가하면 코드가 정상적으로 작동할 것입니다.
