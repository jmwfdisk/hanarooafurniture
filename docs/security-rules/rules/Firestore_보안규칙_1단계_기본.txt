rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 1단계: 기본 규칙 - 로그인한 사용자는 모든 문서 읽기/쓰기 가능
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

